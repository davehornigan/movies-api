runtime: go120
main: cmd/api/main.go
env_variables:
  SERVER_PORT: 8080

handlers:
  - url: /(.*\.(gif|png|jpg))$
    static_files: static/\1
    upload: static/.*\.(gif|png|jpg)$
  - url: /.*
    script: auto
instance_class: B1
basic_scaling:
  max_instances: 1